language: node_js
node_js:
  - "0.10"
branches:
  only:
    - master
    - develop
    - /^release.*$/
notifications:
  email:
    - robbl@moj.io

before_script:
  - npm install -g bower
  
script:
  - ./build.sh