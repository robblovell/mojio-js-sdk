language: node_js
node_js:
  - "0.10"
branches:
  only:
    - master
    - develop
    - /^release.*$/
notifications:
  email:
    - robbl@moj.io

before_script:
  - npm install -g bower@1.3.3
  
script:
  - ./build.sh