// Generated by CoffeeScript 1.9.3
(function() {
  var MojioClient, User, assert, config, mojio_client, should, testObject, testdata;

  MojioClient = require('../../src/nodejs/MojioClient');

  User = require('../../src/models/User');

  config = require('../config/mojio-config.coffee');

  mojio_client = new MojioClient(config);

  assert = require('assert');

  testdata = require('../data/mojio-test-data');

  should = require('should');

  testObject = null;

  describe('User', function() {
    before(function(done) {
      return mojio_client._login(testdata.username, testdata.password, function(error, result) {
        (error === null).should.be["true"];
        return done();
      });
    });
    return it("can't query Users by non-accesible UserName", function(done) {
      return mojio_client.query(User, {
        criteria: {
          UserName: 'robblovell'
        },
        limit: 10
      }, function(error, result) {
        (error === null).should.be["true"];
        mojio_client.should.be.an.instanceOf(MojioClient);
        result.Objects.should.be.an.instanceOf(Array);
        result.Objects.length.should.equal(1);
        return done();
      });
    });
  });

}).call(this);

//# sourceMappingURL=users_test.js.map
